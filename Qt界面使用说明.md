# 抖音爬虫Qt界面使用说明

## 概述
本项目已成功改造为Qt图形界面，提供更友好的用户体验。界面包含七个主要功能模块，支持数据表格显示和自定义保存路径。

## 启动方式

### 1. Qt图形界面（推荐）
```bash
# 方法1：按 Win + R，输入 powershell，回车
# 方法2：按 Win + X，选择 "Windows PowerShell"
# 方法3：在开始菜单搜索 "PowerShell"

cd F:\xxx\Spider\DouYin_Spider

# 激活虚拟环境
.\venv\Scripts\Activate.ps1

python main.py --qt
```

### 2. 控制台界面
```bash
python main.py --console
```

### 3. 直接启动脚本
```bash
python start_qt.py
```

## 功能模块

### 1. 单个视频爬取
- **功能**: 爬取指定视频的详细信息
- **输入**: 抖音视频链接
- **显示**: 视频信息以表格形式展示
- **保存**: 支持JSON和Excel格式

### 2. 用户作品爬取
- **功能**: 爬取指定用户的所有作品
- **输入**: 抖音用户链接
- **显示**: 作品列表以表格形式展示
- **保存**: 支持JSON和Excel格式

### 3. 搜索视频
- **功能**: 根据关键词搜索视频
- **输入**: 搜索关键词、数量
- **显示**: 搜索结果以表格形式展示
- **保存**: 支持JSON和Excel格式

### 4. 视频评论爬取
- **功能**: 爬取指定视频的评论区
- **输入**: 抖音视频链接（支持多种URL格式）
- **显示**: 评论列表以表格形式展示，进度显示区域显示爬取过程
- **保存**: 支持JSON和Excel格式

**支持的URL格式：**
- 标准格式：`https://www.douyin.com/video/1234567890`
- 用户页面格式：`https://www.douyin.com/user/xxx?modal_id=1234567890`

### 5. 直播间监听
- **功能**: 实时监听直播间弹幕、礼物、评论和用户进入
- **输入**: 直播间ID
- **显示**: 实时显示弹幕、礼物、用户进入、点赞等信息
- **保存**: 支持保存监听数据到JSON和Excel文件

**监听内容：**
- 弹幕消息：用户昵称和消息内容
- 礼物信息：送礼者、收礼者、礼物名称和数量
- 用户进入：用户进入直播间的通知
- 点赞信息：用户点赞行为

### 6. 搜索用户
- **功能**: 根据关键词搜索用户
- **输入**: 搜索关键词、数量
- **显示**: 用户信息以表格形式展示
- **保存**: 支持JSON和Excel格式

### 7. 搜索直播间
- **功能**: 根据关键词搜索直播间
- **输入**: 搜索关键词、数量
- **显示**: 直播间信息以表格形式展示
- **保存**: 支持JSON和Excel格式

## 界面特性

### 美观设计
- 渐变背景色
- 支持自定义背景图片（`static/background.jpg`）
- 现代化的按钮和表格样式
- 响应式布局

### 数据展示
- 表格形式展示JSON数据
- 自动调整列宽
- 支持数据排序和选择
- 交替行颜色显示

### 进度显示
- 每个功能模块都有独立的进度显示区域
- 实时显示爬取过程和状态信息
- 显示URL解析、数据获取、保存等步骤
- 支持多行文本显示，方便查看详细进度

### 文件保存
- **自动保存**: 数据爬取完成后自动保存到`datas/功能类型/`目录
- **媒体下载**: 自动下载视频和图片到`datas/media_datas/`目录
- **复制功能**: 保存按钮用于将已保存的数据复制到其他位置
- **双格式**: 同时保存JSON和Excel格式文件
- **时间戳**: 自动生成时间戳文件名

### 多线程处理
- 爬虫任务在后台线程执行
- 界面保持响应
- 实时状态更新
- 错误处理和提示

## 配置要求

### 环境依赖
- Python 3.9+
- PyQt5
- 其他依赖见`requirements.txt`

### Cookie配置
确保`.env`文件包含有效的Cookie配置：
```
DOUYIN_COOKIE=你的抖音Cookie
LIVE_COOKIE=你的直播Cookie
DEFAULT_LIVE_ID=19360448382
```

## 使用流程

1. **启动程序**: 运行`python main.py --qt`
2. **选择功能**: 点击对应的选项卡
3. **输入参数**: 填写必要的URL或关键词
4. **开始爬取**: 点击"开始"按钮
5. **查看结果**: 在表格中查看爬取的数据
6. **自动保存**: 数据自动保存到`datas/功能类型/`目录
7. **媒体下载**: 视频和图片自动下载到`datas/media_datas/`目录
8. **复制数据**: 点击"复制数据到其他位置"按钮将数据复制到其他位置

## 注意事项

1. **网络连接**: 确保网络连接稳定
2. **Cookie有效性**: 定期更新Cookie配置
3. **数据量**: 大量数据爬取可能需要较长时间
4. **文件权限**: 确保有写入权限到保存目录

## 故障排除

### 常见问题
1. **界面无法启动**: 检查PyQt5是否正确安装
2. **爬取失败**: 检查网络连接和Cookie配置
3. **数据不显示**: 检查输入URL格式是否正确
4. **保存失败**: 检查目标目录权限

### 错误提示
- 程序会显示详细的错误信息
- 状态栏显示当前操作状态
- 弹窗提示重要信息

## 技术特点

- **模块化设计**: 每个功能独立实现
- **异步处理**: 使用QThread避免界面卡顿
- **数据转换**: 自动将JSON数据转换为表格显示
- **错误处理**: 完善的异常处理机制
- **用户友好**: 直观的操作界面和提示信息

## 更新日志

- **2025年**: 完成Qt界面改造
- 支持七个主要功能模块
- 实现数据表格显示
- 修复信号类型错误，支持list和dict数据
- 修复"list index out of range"错误，增强数据安全性
- 修复搜索视频功能中的方法调用错误
- 优化控制台日志显示，减少下载过程的冗余输出
- 添加实时进度显示功能，在红框区域显示爬取过程
- 修复评论爬取时的URL解析问题，支持modal_id参数
- 完善直播间监听功能，支持实时显示弹幕、礼物、评论和用户进入
- 实现直播间监听数据保存功能，支持JSON和Excel格式
- 实现自动保存功能，数据自动保存到默认位置
- 添加媒体文件自动下载功能，视频和图片保存到media_datas目录
- 保存按钮改为复制功能，可将数据复制到其他位置
- 美化界面设计
- 集成原有爬虫功能

---

**作者**: 五更琉璃  
**日期**: 2025年  
**版本**: 1.0
