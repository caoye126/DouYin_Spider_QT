#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
默认选项功能演示说明
"""

功能说明：
==========

1. 【菜单选项 - 10秒超时，默认值为2】
   - 位置：程序启动后的主菜单选择界面
   - 超时时间：10秒
   - 默认值：2（爬取用户所有作品）
   - 工作方式：
     * 显示菜单后，提示 "请选择功能 (0-7) (默认值: 2, 10秒后自动使用默认值): "
     * 用户有10秒时间输入选择
     * 如果10秒内没有输入，自动使用默认值2，继续执行爬取用户所有作品
     * 如果用户输入内容，立即使用用户输入的值

2. 【保存方式 - 10秒超时，默认值为media】
   - 位置：爬取用户作品时的保存方式选择
   - 超时时间：10秒
   - 默认值：media（下载视频和图片）
   - 工作方式：
     * 提示 "保存方式 (all/media/excel) (默认值: media, 10秒后自动使用默认值): "
     * 用户有10秒时间输入选择
     * 如果10秒内没有输入，自动使用默认值media，继续执行下载
     * 如果用户输入内容，立即使用用户输入的值

可选值说明：
============

菜单选项 (0-7)：
  0 - 退出程序
  1 - 爬取单个视频信息
  2 - 爬取用户所有作品 [默认]
  3 - 搜索视频
  4 - 爬取视频评论区
  5 - 监听直播间
  6 - 搜索用户
  7 - 搜索直播间

保存方式 (all/media/excel)：
  all    - 保存所有信息（包括视频/图片、Excel）
  media  - 只保存视频和图片 [默认]
  excel  - 只保存到Excel
  media-video  - 只下载视频
  media-image  - 只下载图片

使用场景：
==========

场景1：无人值守爬虫
- 启动程序后，10秒内不操作，自动选择功能2（爬取用户所有作品）
- 输入用户URL后，10秒内不操作，自动选择保存方式media（下载媒体文件）
- 程序继续自动执行

场景2：快速使用
- 用户可以快速按下选项（如输入3搜索视频），无需等待10秒

实现细节：
==========

- 使用threading库实现超时控制
- 采用daemon线程，不会阻止程序退出
- 超时后自动使用默认值，提示用户选择了默认值
- 输入验证失败时也会使用默认值
